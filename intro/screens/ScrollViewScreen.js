import { Text, StyleSheet, View } from 'react-native'

export default function ScrollViewScreen() {
    return (
      <View>
        <Text>Proximamente por Barbara & equipo</Text>
      </View>
    )
}

const styles = StyleSheet.create({

})